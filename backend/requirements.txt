# scraper core
curl_cffi>=0.7        # browser-impersonating HTTP transport (bypasses Cloudflare)
aiohttp>=3.9          # used by the test mock server
beautifulsoup4>=4.12
ebooklib>=0.18
PyYAML>=6.0

# api layer
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9   # multipart/form-data (EPUB upload)
sse-starlette>=2.1
sqlmodel>=0.0.22

# optional: local text-to-speech (Kokoro)
kokoro-onnx>=0.4
# TTS audio is transcoded WAV->MP3 (~6x smaller, much faster to stream to phones)
# via the system `ffmpeg` binary when present. Not a pip package: install it from
# your OS (e.g. `pacman -S ffmpeg`). Falls back to serving WAV if absent.

# tests
httpx>=0.27
