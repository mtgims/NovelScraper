org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
org.gradle.configuration-cache=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# The app's version, shared by the Android APK (androidApp) and the desktop app
# (composeApp). Bump BOTH for every release: appVersionCode must increase for
# Android to accept the install over a previous one; appVersionName is what
# people see. See CHANGELOG.md.
appVersionCode=98
appVersionName=0.48.0
