<?xml version="1.0" encoding="utf-8"?>
<!-- The Android app shell. Permissions, the narration service and the media
     button receiver come from the shared module's manifest (composeApp). -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.NovelScraper">

        <!-- Upright only. Laid on its side a phone is wide enough in dp to cross
             the threshold where the layout switches to the one meant for a
             window on a computer, sidebar and all, which is not what a phone
             wants. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.NovelScraper">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
